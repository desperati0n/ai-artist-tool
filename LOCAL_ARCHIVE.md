# 本地存档模式

本地存档模式把元数据和原图写入项目的 `data/` 目录，不再把新图片长期保存在浏览器 IndexedDB 中。

## 启动

在项目目录运行：

```powershell
python run_server.py
```

然后访问：

```text
http://localhost:8010/index.html
```

页面左侧显示“本地存档 · data/”即表示连接成功。直接双击 HTML 或使用 GitHub Pages 时，工具会继续使用原有浏览器存储作为回退。

## 文件结构

```text
data/
├── data.json       # 画师、分类、预设、主题
└── images/         # 按画师/预设 ID 保存的原始图片文件
```

- 元数据采用临时文件加原子替换，避免保存中断留下半个 JSON。
- 普通上传保留原文件格式和原始字节，不缩放、不重新编码。
- NAI 插件生成图保持原分辨率，写入前转换为质量 `0.94` 的 JPEG，以清除生成参数并减小体积。
- 如果 JPEG 转换不可用，会保留原图；PNG 上传接口可以无损移除文本、EXIF 和时间块，不重编码像素数据。

## 首次迁移

当 `data/data.json` 尚不存在时，页面会把当前浏览器里的元数据和图片复制到 `data/`。迁移不会自动删除浏览器旧副本。

迁移成功后，可以点击左侧“释放旧缓存”。工具会先确认所有仍被引用的浏览器图片在 `data/images/` 中都有对应文件，验证通过并再次确认后才清空 IndexedDB 图片。

## 备份与恢复

本地模式的备份和恢复都直接在网页完成，不需要手工解压、复制或替换 `data/`。点击“导出存档”后，由 Python 服务生成并下载 ZIP：

```text
artist_manager_backup_YYYY-MM-DD_HHMMSS.zip
├── manifest.json
└── images/
```

图片以二进制原文件进入 ZIP，不会转换成 Base64，也不会一次性进入浏览器 JavaScript 内存。

点击“导入存档”并选择该 ZIP 后，页面会显示上传进度；上传完成后，服务会在临时目录校验元数据和图片、生成缩略图，再原子切换到新存档。任何步骤失败都会保留原有 `data/`，成功后页面自动刷新。

ZIP 恢复会完整替换当前本地存档，旧版 JSON 仍可导入并继续采用合并模式。JSON 仅用于兼容旧备份；日常完整备份推荐使用网页生成的 ZIP 存档。

## 注意

- 请通过 `run_server.py` 访问页面；本地存档 API 只监听 `localhost:8010`（可用 `AI_ARTIST_PORT` 覆盖）。
- 不要在服务运行时手工编辑 `data/data.json`。
- `data/` 是主存档，仍建议定期复制整个目录或导出 ZIP 到其他磁盘。
- ZIP 恢复属于完整恢复，不是合并；JSON 导入才是合并。
