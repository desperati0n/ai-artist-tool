# Changelog

## Unreleased

### 批量生成与审查性能

- 生成、暂停、停止和通过/拒绝例图改为局部更新，保留画师、参数和审查列表容器；保存中的卡片不再消失，完整刷新也保留各列滚动位置。
- 成功后立即串行生成下一张，移除每张固定 350ms 等待；失败时退避并尊重 Retry-After，认证失败时停止队列。
- “全部通过”只执行一次元数据镜像和后台画廊刷新，保存重试复用已转换的 JPEG；保留人工确认、筛选与单请求单张策略。
- 请求记录增量追加，保留展开状态和阅读位置，并记录请求、解包、转换、写入与队列总耗时。

### 界面修正与启动入口

- 修复 React SVG 搜索图标的垂直居中，保持搜索框高度稳定。
- 统一例图卡片光效与内侧圆角，移除边缘裁切的阴影，并按卡片边框和缩放校正指针坐标。
- Python 服务启动后自动打开 React 新版，根地址跳转 `/react.html`；保留 `/index.html`，支持 `--no-browser`。

### React + TypeScript + Vite 重构

- 新增 `react.html` 和按功能组织的 `src/`，同时完整保留原有 `index.html`，便于并行验证与回退。
- 将画师、分类、Prompt、预设、生图、存储、导入导出和网络请求拆分成独立模块；Python 本地服务同步拆为 `server/` 包。
- 保留按钮指针扩散、卡片聚光、分类滑动指示、权重饼图、画师悬浮预览和拖拽排序，并为关键交互加入回归测试。
- 保持原有 LocalStorage、IndexedDB 和 `data/` 存档格式，Python 服务可直接托管 `npm run build` 生成的 React 页面。
- 修复预设保存未采用显式拖拽顺序，以及悬浮预览名称可能被当作 HTML 解析的问题。
- 新增统一的 `npm run check`，覆盖 React 工作流、存储兼容、旧 JS 测试、Python 测试、类型检查和生产构建。

### 画师数据

- 导入画师时统一忽略名称中的大小写、空格和下划线差异，`A(apple)`、`A_(apple)`、`A (apple)` 现在只保留一位画师。
- 启动时自动合并旧存档中的同名重复画师，并保留首条记录的 ID 与图片，同时合并分类、热度、社交链接和预设引用。
- 手动新增、Prompt 导入和批量生图收录复用同一身份规则，避免从其他入口再次产生重复画师；星号仍按名称中的有效字符处理。

### NAI 批量生图插件

- 将生成期间的完整界面重建改为每 10 张一次，同时保留逐张进度更新和队列结束时的最终刷新。

## [v15.1] - 2026-08-22

### NAI 批量生图插件

- 新增画师“有例图”状态标记，并支持当前分类内一键选择无例图画师。
- “全选”严格限制在当前分类和搜索结果，避免误选其他分类的画师。
- Prompt 编排区新增“送到生图区”：粘贴他人画师串后，自动收录缺失画师，并只发送没有例图的画师。
- 当前画师串全部已有例图时自动隐藏“送到生图区”按钮。
- 通过审核的图片直接写回 IndexedDB 画师资料；拒绝后可保留选择继续生成。
- API Key 在当前页面会话内保留，关闭插件窗口后重新打开无需重复粘贴；刷新页面后清除，不写入导出文件。
- 更新默认正面提示词为固定海滩构图提示词，并保留 `{artist}` 占位符。

## [v15] - 2026-08-14

### UI 重设计

- 将 v15 黑曜石 × 骨白界面收敛为唯一入口 `index.html`，保留原有数据与功能。
- 重构为以工具效率为核心的三栏工作区，压缩装饰性内容，强化搜索、分类、画师选择与 Prompt 编排之间的操作关系。
- 统一浅色与深色主题的面板、边框、间距、排版和响应式抽屉行为。
- 将默认 Prompt 输出格式调整为 NAI，同时保留 SDXL/NAI 手动切换能力。
- 为左侧分类 Dock 增加跟随当前分类的滑动指示动画，并兼容键盘焦点与减少动态效果偏好。
- 为画师卡片增加指针跟随的中性聚光边缘反馈，保持卡片点击、批量选择、编辑和删除逻辑不变。

### 按钮与视觉系统

- 引入从指针位置扩散的侵蚀填充交互，并统一按钮的 Hover、按下、禁用与键盘焦点状态。
- 将原有蓝色强调体系替换为“黑曜石 × 骨白”：浅色模式使用黑曜石主按钮和骨白文字，深色模式反转为骨白按钮和深色文字。
- 同步更新主操作、NAI 切换、分类选中态、焦点环、数据图表与卡片光晕，移除残留的高饱和蓝色视觉冲突。
- 修复深色与浅色模式下部分按钮 Hover 后填充色与文字颜色接近、导致文字不可读的问题。
- 修复“存预设”图标缺失以及“存预设”“清空 Prompt”等按钮内容未居中的问题，并统一检查页面、卡片、翻页和弹窗按钮的视觉对齐。
- 为纯图标按钮补充可访问名称与提示文本，改善键盘和辅助技术使用体验。

### 验证

- 验证桌面端、390px 移动端以及浅色/深色主题下的布局、按钮对齐和文字对比度。
- 确认移动端 Prompt 抽屉无文字换行、遮挡或横向溢出。
- 通过全部内联 JavaScript 语法检查。

## [v14.0] - 2026-06-19

### ✨ 新功能 (New Feature)

#### 网页端一键更新数据功能 (Built-in Danbooru Data Updater)
- **免 Python 环境直连抓取**：将原 `fetch_danbooru_counts.py` 的核心抓取机制全量移植为纯前端 JavaScript，不再依赖外部 Python 脚本，支持在网页内直接更新数据。
- **全新更新控制面板**：
  - 支持 **仅未更新** (增量)、**全部更新** (强制覆写)、**仅补链接** (只增补缺失的社交媒体链接) 三种抓取模式。
  - 提供了 **API 限制速率滑块** (1~6 req/s) 与 **跳过社交链接** 设置，由用户自由把控更新流量，降低被封锁风险。
  - 实时反馈任务百分比、进度条、实时速度（Speed/s）和剩余时间（ETA），提供滚动日志输出。
  - 增加 VPN 连接状态引导警告，指导国内网络环境下的用户配置系统代理。
- **并发与限流保障**：
  - 基于异步 Promise 并发池（控制最大并发）和 Token Bucket 限流算法，确保请求不会遭遇 API 429 报错。
  - 运行更新任务时，阻止外部组件重绘打断弹窗，并提供每处理 20 条数据自动落盘机制，以及手动随时取消机制。
- **配套架构文档**：
  - 根目录下新增 `architecture_reference.md` 学习指南，方便二次开发。

## [v13.0] - 2026-04-09

### ✨ 新功能 (New Feature)

#### Danbooru 画师社交链接智能抓取脚本 (`fetch_danbooru_counts.py`)
- **全新社交链接收录**：脚本不仅支持抓取画师热度（Count），现已深度结合 Danbooru 两段式 API 收集画师最新全网社交链接。
- **两阶段精准查库机制**：修复了旧有单步查询导致的无差别获取全站脏数据的问题；改为严格通过 `获取 artist_id -> 根据 ID 获取所属链接` 的链路，彻底杜绝数据错乱关联。
- **自动去重逻辑**：自动抹脱爬取链的末尾残缺斜杠进行全自动剔重比对（Deduplication），杜绝无意义冗余。
- **内置代理直连支持**：重写了底层网络收发器应对 GFW，通过 `--proxy` 完美支持科学上网端口穿透，同时下调高并发吞吐降低遭遇防爬墙 403 拦截与连接爆破（Connection Reset）的风险点。

#### 网页端：智适应无损导入更新系统 (Smart Upsert Import)
- **增量热重载更新**：主网页读取 `json` 备份恢复时，丢弃了“遇到本地已有画师则彻底忽略跳过”的陈旧逻辑。
- **精准合并融合**：当识别到已有存在卡片时，将最新引入的热度值 (`danbooruCount`)、多维社交链接数组（`socialLinks`）、重组并集化归类标签 (`categories`) **直接覆写注入现有卡片中**。
- **解放手动删库繁琐**：此后更新进度无须清空主页面画师！导入更新动作将绝对不会摧毁你原自设的心血版型图（`imageUrl` 与 IndexedDB 持久态）以及 UUID 唯一键！
- **导入报告弹窗**：`showToast` 通报器现会精准剖析本次传输中「几位新增」以及「几位被后台更新」。

### 🔧 视觉与交互改进 (UI & Tweaks)
- 彻底移除了卡片底部社交链接池大于 4 个图标时的暴力 `+X` 文字截断。
- 引入附带宽幅柔性渐变蒙版（Mask-image fade-out）与水平防误触滑动槽（overflow-x-auto & scrollbar-hide），灵活兼容容纳所有画师任意数量平台链接的水平阅览。

## [v12.1] - 2026-04-07

### ✨ 新功能 (New Feature)

#### 画师串反向导入 (Prompt String Import)
- 右侧「咒语生成」区域的文本框改为**可编辑**，支持直接粘贴画师串
- 新增琥珀色 **「导入串」** 按钮，点击后自动解析并填充右侧画师栏
- **解析格式支持**：
  - NAI 加权格式：`0.8::artist1, artist2::` → artist1/artist2 权重 0.8
  - NAI 混合格式：`0.8::artistA::, artistB, 1.2::artistC, artistD::` → 各自权重
  - NAI `artist:` 限定词前缀：`artist:nixeu` → 自动剥除前缀识别为 `nixeu`
  - WebUI 加权格式：`(artist_name:1.1)` → 权重 1.1
  - 普通逗号分隔：`artist1, artist2` → 默认权重 1.0
- **匹配策略**（多层容错）：
  1. 精确匹配 tag 或 name（不区分大小写）
  2. DB tag 带 `artist:` 前缀但输入不带（自动适配）
  3. 输入带 `artist:` 前缀但 DB 不带（反向适配）
- **未匹配画师**显示为 Ghost 占位卡（虚线边框 + 🔶「未匹配」徽章），权重可调、可删除
- 导入完成后显示 Toast 通知，提示已匹配数量和未找到占位数量
- 清空按钮同步清理所有 Ghost 占位条目

### 🐛 Bug 修复 (Bug Fixes)

#### 修复 NAI 解析器正则匹配错误
- **问题 1（数字歧义）**：旧正则 `[\d]*\.?[\d]*` 可匹配空字符串，导致字符串中任意 `::content::` 均能被视为带权重的 NAI 组，引发无权重内容被错误分组
  - **修复**：权重改为严格 `\d+(?:\.\d+)?`，要求至少一位数字，不再匹配空字符串
- **问题 2（`artist:` 前缀）**：旧版不处理 NAI 的 `artist:` qualifier，导致 `artist:nixeu` 在 DB 中查找 `artist:nixeu` 而非 `nixeu`，命中率为零
  - **修复**：新增 `stripArtistPrefix()` 函数，解析阶段统一剥除 `artist:` 前缀；查库时也尝试带/不带前缀两种形式
- **问题 3（单冒号画师名导致 NAI 组解析失败）**：旧正则内容部分 `[^:]+?` 遇到**任何** `:` 就中止，导致画师名含单冒号时（如 `artist:bm94199`）整个 NAI 组无法识别，进而产生三个错误副产物：
  - `1.3:: artist:bm`（错误的 bare text）
  - `1.7`（被错误识别为 NAI 组的"内容"）
  - `nixeu ::`（未被识别为组成员、以 bare text 处理后残留结尾 `::`)
  - **修复**：内容部分改为 `(?:[^:]|:(?!:))+?`，使用负前瞻 `(?!:)` 区分单冒号（允许）与双冒号（停止），彻底解决单冒号画师名解析问题

### 🔧 其他改进 (Other Improvements)

- 修复卡片选中样式：CSS 中 `ring: 3px` 改为标准属性 `outline: 3px`+`outline-offset: 2px`（Tailwind 的 `ring` 不能直接用于 `<style>` 块）
- 右侧空画师栏提示文字更新，增加「粘贴画师串导入」引导
- 完善 HTML `<head>` SEO 元数据（meta description、Open Graph、内联 SVG Favicon）
- NAI 格式输出去掉多余空格：`${w}:: ${tag} ::` → `${w}::${tag}::`

---

## [v12.0] - 2026-03-21

首个正式版本。核心功能：IndexedDB 图片存储、画师 Tag 组合与权重调节、饼图可视化、拖拽排序、批量管理、风格预设系统。
